<div class="shop_menus_container">
  <!-- <ul>
    <% @shop_menus.each do |menu|%>
      <li>
        <%= image_tag "/user_picts/ra-men.jpg", size: 50, class: "menu_pict" %>
        <%= link_to menu.name, menu_path(menu), class: "menu_name" %>
        <span class="menu_price"><%= menu.price %></span>
      </li>
    <% end %>
  </ul> -->
  <div class="shop_menu_header">
    <%= link_to @shop.name, shop_path(@shop) %>
    <span> > メニュー</span>
  </div>
  <ul>
    <% @shop_menus.each do |menu|%>
    <li>
      <%= link_to(menu_path(menu), class: "shop_menu_container") do %>
        <%= image_tag "/user_picts/noimage.png", size: 30, class: "menu_pict" %>
          <span class="menu_name"><%= menu.name %></span>
          <span class="menu_price clearfix"><%= menu.price %>円</span>
      <% end %>
    </li>
    <% end %>
  </ul>
</div>
